{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(cppdebug) Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "osx": {
        "MIMode": "lldb"
      },
      "preLaunchTask": "CMake: build"
    }
  ]
}