{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(cppdebug) Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/EmptyCppProject",
      "cwd": "${workspaceFolder}",
      "osx": {
        "MIMode": "lldb"
      },
      "build": "CMake: build",
      "preLaunchTask": "CMake: build"
    }
  ]
}
